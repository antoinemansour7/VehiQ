def take_deposit(user_id, amount):
    print(f"Taking a deposit of {amount} CAD for user {user_id}")
    # Mocking a successful payment gateway interaction
    return True, "Deposit successfully processed."
